---
description: 开发在本地开发分支进行；仅当用户明确说「合并到主分支」时才合并主分支，合并后回到原分支
alwaysApply: true
---

# Git：开发分支与合并门禁

## 日常开发（执行类任务）

- **不要在主分支上直接改代码、提交**。主分支指仓库的 **`main` 或 `master`**（动手前用 `git branch` / `git status` 确认当前默认主干名称）。
- **在本地开发分支上修改与提交**，例如已有的 `develop` / `dev`，或本次任务专用的 `feature/…` / `fix/…` 等**非主分支**。
- 若当前**正停在主分支**且用户要求开始开发：先**创建并切换**到合适的开发分支，再编辑与 `git commit`。
- 推送、PR、变基等仍按用户指示；本规则不代替用户设定远程策略。

## 合并到主分支（强门禁）

- **仅当**用户在请求中出现**连续子串「合并到主分支」**（六字，例如「请**合并到主分支**」）时，才执行**将当前开发工作合并进主分支**的操作（例如 `checkout` 主分支 → `merge` 开发分支 → 必要时 `push`）。
- 仅有同义说法（如「合进 main」「merge 到主干」「发布到生产」）而**不出现**上述六字子串时：**不要**擅自合并进主分支，即使开发已「完成」。

## 合并完成后的分支

- 执行「合并到主分支」前，**记下当前分支名**（一般为正在开发的那个分支，记为「原分支」）。
- 合并相关操作**全部完成后**，必须 **`git checkout` 回到原分支**，再继续后续工作或向用户汇报，避免停留在主分支上继续改代码。

## 简短示例

- 用户在 `feature/foo` 上让你做功能 → 全程在 `feature/foo` 提交，**不**合并到主分支。
- 用户说「**合并到主分支**」→ 按门禁执行合并流程，结束后 **checkout 回 `feature/foo`**（或合并前记录的那个分支）。
